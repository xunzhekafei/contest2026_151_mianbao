############################################################################
# app/led_test/Make.defs
############################################################################

ifneq ($(CONFIG_LED_TEST),)
CONFIGURED_APPS += $(APPDIR)/examples/led_test
endif
